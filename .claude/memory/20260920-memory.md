# Long-term Memory — 2026-09-20

## Prompts
- User: "use a skill orchestrator nesse repo" → ran `/orchestrator` → Phase 0 preconditions, chose harness bootstrap + audit.
- Decisions via gate: delete 3 untracked leftovers (`fix_gui.py`, `remove2.sh`, `test_docker.sh`); enable GitHub Issues; bootstrap full harness; adopt Conventional Commits; target Claude + Devin only.

## Decisions
- Deleted `fix_gui.py` (IDE-menu patch already applied to `distro/gui.sh`), `remove2.sh` (stale inferior variant of `remove.sh`), `test_docker.sh` (helper superseded by `termux-test.yml` + testing skill). User-approved.
- `.devin/skills/testing-modded-ubuntu/` kept — admissible platform dir, high-value E2E knowledge.
- Harness single source: `CLAUDE.md`; Devin imports `.claude/` via `.devin/config.json` `read_config_from.claude`. No `AGENTS.md` generated (user opted out).
- Commits: Conventional Commits adopted (repo previously used pt-BR free prose).

## Checkpoints
- `gh repo edit afonsoft/modded-ubuntu --enable-issues` — OK; `gh issue list` → `[]`.
- Branch `feature/devin-20260920-bootstrap-claude-harness` created from clean `master`.

## Lessons learned
- `gh repo view` resolves to upstream parent `modded-ubuntu/modded-ubuntu` — always pass `--repo afonsoft/modded-ubuntu` explicitly.
- Rootfs path in termux-docker container: `${PREFIX}/var/lib/proot-distro/containers/ubuntu/rootfs` (see testing skill).

## Session summary
- Harness bootstrap in progress on `feature/devin-20260920-bootstrap-claude-harness`: CLAUDE.md, settings.json, rules (global + shell-scripts), 4 sub-agents, memory protocol, CONTEXT/RULES/MEMORY/TOOLS/WORKFLOWS/README, dod command, docs/*, .devin/config.json. Pending: validation + Phase 2 audit + commit/PR.
