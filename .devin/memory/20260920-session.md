# Session — 2026-09-20 (Devin native memory)

**Scope**: `/orchestrator` run — harness bootstrap + repo audit on `afonsoft/modded-ubuntu`.

**Key facts for future sessions**:
- Harness single source: `CLAUDE.md` + `.claude/` — Devin imports via `.devin/config.json` (`read_config_from.claude`).
- Orchestrator state lives in `.claude/memory/orchestrator_stats.md`; session history in `.claude/memory/orchestrator_sessions.md`; daily memory in `.claude/memory/{YYYYMMDD}-memory.md`.
- Commits: Conventional Commits (`feat:`/`fix:`/`docs:`/`refactor:`) — adopted 2026-09-20, replacing pt-BR free prose.
- GitHub Issues enabled on `afonsoft/modded-ubuntu` (was disabled).
- Always pass `--repo afonsoft/modded-ubuntu` to `gh` — default resolution picks the upstream parent `modded-ubuntu/modded-ubuntu`.
- E2E testing guide: `.devin/skills/testing-modded-ubuntu/SKILL.md` (termux-docker, rootfs at `containers/ubuntu/rootfs`, `-u system` for pkg, wrapper scripts for env vars).
- Verification chain: `bash -n` → `shellcheck -S warning` → `xmllint` → docker E2E → `/dod` command wraps it.
- Open gap: GAP-005 — no `docs/architecture/` ADRs yet.
- Protected: `master` (feature branches only), `.github/workflows/` (approval), `distro/proot-distro.sh` (vendored, immutable).
